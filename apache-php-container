1. Download container image
    >podman login docker.io
    >podman pull php:7.4-apache

2. list locally stored images
    >podman images

3. inspect locally stored image and return info 
    > podman inspect registry.redhat.io/rhel8/php-80

4. run podman container 
    >podman run -dit --name apache-php -p 80:80  registry.redhat.io/rehl8/php-80:tag_name    /bin/bash

5. list all containers in host 
    >podman ps -a

6. stop podman container
    > podman stop apache-php

7. remove container 
    >podman rm apache-php

8. enter into container using bash shell
    >podman exec -it apache-php /bin/bash

9. Remove a locally stored image using the podman rmi command
    >podman rmi image_name 

10. export image to tar
    >podman save -o php.tar docker.io/php:7.4-apache 

11. load image from tar
    >podman load -i php.tar